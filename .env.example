# Copiar a .env y ajustar. .env no se versiona.
SECRET_KEY=cambia-esto-por-una-clave-larga-y-aleatoria
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# Si se omite, el proyecto usa SQLite para que corra sin instalar PostgreSQL.
# DATABASE_URL=postgres://usuario:clave@localhost:5432/incidencias_ti

# En produccion (AWS), ademas:
# ALLOWED_HOSTS=mi-dominio.cl
# CSRF_TRUSTED_ORIGINS=https://mi-dominio.cl
